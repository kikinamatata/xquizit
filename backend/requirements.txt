fastapi
uvicorn
langchain
langchain-google-genai
langgraph
requests
python-multipart
PyPDF2
python-docx
pydantic
pydantic-settings
python-dotenv
numpy>=1.26.0
scipy>=1.12.0
sse-starlette
nest-asyncio
tenacity
prometheus-client
websockets

# Embedded Whisper Transcription
faster-whisper>=1.0.0
ctranslate2>=4.0.0
onnxruntime>=1.17.0
tokenizers>=0.20.0
huggingface_hub

# Core PyTorch and ML dependencies (required for TTS)
# For CUDA 12.8 support, install with: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu128
# For CPU-only (Windows/macOS), the default pip install works
torch>=2.5.0
torchaudio
transformers>=4.46.0
soundfile>=0.12.0
accelerate

# Chatterbox TTS (Real-Time Streaming Text-to-Speech)
# Install manually with: pip install chatterbox-tts
# Note: chatterbox-tts is not included in this requirements.txt and must be installed separately
